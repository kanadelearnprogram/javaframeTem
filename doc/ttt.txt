六、上机，前五个要求为基础要求，后五个要求为提高要求。
只要这十点要求都符合，考试只需要改一些数据表、模型类和一些特定的sql语句。
五个基础为35，五个提高为15。
只要十点都有都对，上机五十分没有问题。
与考试要求无关的代码功能不要提交，避免框架雷同。
只能使用SpringMVC+mybatis，禁止使用三框架，禁止使用springboot。
上机考试不仅要写项目，还要写一个文档报告，文档报告内容包括对项目功能代码的截图，对项目结果的演示截图
考试项目上传QQmail：99867460@qq.com
只能SpringMVC+mybatis，禁止使用三框架，禁止使用springboot
1.要求在数据库操作中的模型和数据表阶段
要有日期类型的数据，有两个考察点
第一个，数据库里的日期类型怎么存放
第二个，从页面提交数据时，日期的格式
默认的情况下springmvc的数据类型转换是yy/mm/dd这种格式，是自动的
但是一旦输入mm-dd-yy，这个时候类型怎样自动转换（提示，要用到一个注解叫做date）
写在接受参数的模型string上。

2.查询 mybatis的查询有很多种，其中用的比较多的是模糊查询
怎样给模糊查询传参？ 模糊值的参值写在哪 ？写在参数里，代码里还是写在sql语句里？
在模糊参数中有两个，一个是问号#{}一个是反问号${}，用哪个？

3.查询 分页
第一，总记录数怎么获取 ，涉及到一个聚合函数，每页显示几个，怎样计算出查询页
第二，操作语句limit，limit里有两个参数，第几页，每页显示几个。最后用总页数生成一个超链接，下面的页数要有点，要有翻页，例如1、2、3......9、10

4.路径映射加跳转
controller很简单，关键是跳转。在跳转时用的比较多的是控制类直接跳转页面
在跳转页面的时候要注意上层跟下怎么写 。 ..  、..\  的用法。
能不能一个控制类跳到另一个控制类。例如做了一个插入，直接跳转到查询行不行，然后从查询控制类再找。

5.路径传参（必考）
传一个参 传两个参 传多个参 怎么写
路径前台怎么写，后台怎么操作，后台当中怎么样用路径传参定义controller的mapping

6.图片预览怎么写（这个模块不做上传下载，直接预览）
图片预览时，例如显示一个学生，还能显示他的照片。图片自己准备好。图片怎么存放在数据表中？ 在页面中怎么显示 ？可以建一个pic文件夹放几条数据和图片，然后在页面上浏览

7.在文件的删除和更新的时候
做一个确认的对话框。例如在删除时，调用了一个js脚本，弹出对话框“确认删除数据？”有按钮“确认”“取消”


8.添加数据
调用mybatis将数据添加进去，
在mybatis中的插入语句中有一个 回查是insert和select连用。
也就是在插入时自动获取插入对象的数据

9.多表查询
  将分类的主表当中的数据封装在下拉列表，复选框，单选按钮
例如查询分类有分类数据（按照标签查询）。也就是说分类项可以封装成主键而不是单纯显示成表。
 注意下拉菜单不是写死的，他的数据选项是从数据库里查出来的，包括单选按钮也是从数据库里查出来的
要求在多表查询中显示时，可以把主类当中封装成下拉列表或单选按钮
再选下拉列表或按钮后，可以查询出主类的下一类。注意这不是二次查询，一次就要查询到位，也就是在页面中拆解。如果一次查询实在做不出来也可以做二次查询，但是分不高。

10.（较难）（也许不会考）使用拦截器做出日志操作
也就是说在操作之后把操作的时间，操作参数以日志的形式记录。
日志用拦截器做最好，用其他方式做也可以。
例如通常题目中会提到记录下来删除的数据
要求不严格，只要会记录操作之后的数据，以日志形式记录，这条就合格，用不用拦截器无所谓。

